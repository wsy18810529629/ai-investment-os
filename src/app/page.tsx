import { HomeExperience } from "@/components/home/home-experience";

/**
 * The Today page is the MVP landing surface for AI Investment OS.
 * It intentionally starts with daily understanding rather than portfolio value or trading actions.
 */
export default function Home() {
  return <HomeExperience />;
}
